diff --git a/src/components/palette/ColorCard.vue b/src/components/palette/ColorCard.vue
index 7cb9b6e..0e59495 100644
--- a/src/components/palette/ColorCard.vue
+++ b/src/components/palette/ColorCard.vue
@@ -1,11 +1,31 @@
 <template>
-  <div class="color-card" :style="{ backgroundColor: color }">
-    <span class="hex">{{ color }}</span>
-  </div>
+  <button
+    class="color-card"
+    type="button"
+    :style="{ backgroundColor: color }"
+    :aria-label="`Copy ${color} to clipboard`"
+    @click="copyColor"
+  >
+    <span class="hex">{{ copied ? 'Copied!' : color }}</span>
+  </button>
 </template>
 
 <script setup lang="ts">
-defineProps<{ color: string }>();
+import { ref } from 'vue';
+
+const props = defineProps<{ color: string }>();
+const copied = ref(false);
+let copiedResetTimer: ReturnType<typeof setTimeout> | undefined;
+
+const copyColor = async () => {
+  await navigator.clipboard.writeText(props.color);
+
+  copied.value = true;
+  clearTimeout(copiedResetTimer);
+  copiedResetTimer = setTimeout(() => {
+    copied.value = false;
+  }, 1200);
+};
 </script>
 
 <style lang="scss" scoped>
@@ -16,6 +36,15 @@ defineProps<{ color: string }>();
   justify-content: center;
   height: 100vh;
   position: relative;
+  padding: 0;
+  border: 0;
+  font: inherit;
+  cursor: pointer;
+
+  &:focus-visible {
+    outline: 3px solid white;
+    outline-offset: -4px;
+  }
 
   .hex {
     padding: 0.5rem 1rem;
